{% extends 'base.html.twig' %}
{% block meta %}
<meta name="description" content="Action de profil">
{% endblock %}

{% block title %}Profile{% endblock %}


{% block body %}
<div class="encardC">
    <div class="topPage">
        <a class="looksLikeAButtonScarlet" href=" {{path("modifyCredentials", {'id' : app.user.id}) }}">Modifier vos informations de profil</a>
    </div>
    {% if is_granted("ROLE_ADMIN") %}
    <div class="topPage">
        <a class="looksLikeAButton" href=" {{path( "app_register") }}">Créer un profil pour un formateur</a>
    </div>
    <div class="topPage">
        <a class="looksLikeAButtonScarlet" href=" {{path( "deleteProfileFormateur") }}">Supprimer un profil formateur</a>
    </div>
    {% endif %}

    {% if is_granted("ROLE_FORMATEUR") %}
    <div class="topPage">
        <a class="looksLikeAButtonScarlet" href=" {{path( "deleteProfileFormateurSelf" , {'id' : app.user.id }) }}">Supprimer votre profil</a>
    </div>
    {% endif %}
 

    </div>
</div>
<script src="{{ asset('js/toggleEncardC.js') }}"></script>    
{% endblock %}
